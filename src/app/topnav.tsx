import { Container } from "_/components/container";
import { Navbar, NavbarContent, NavbarTitle } from "_/components/navbar";

const Topnav = () => (
  <Navbar>
    <Container>
      <NavbarContent>
        <NavbarTitle>Dockerium</NavbarTitle>
      </NavbarContent>
    </Container>
  </Navbar>
);

export default Topnav;
